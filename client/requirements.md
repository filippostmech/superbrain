## Packages
framer-motion | Complex animations and page transitions
date-fns | Date formatting for posts
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for forms
clsx | Class name conditional logic
tailwind-merge | Merging tailwind classes safely

## Notes
- Tailwind config needs to support the LinkedIn color palette
- Using shared/routes for all API calls
- Auth handled via useAuth hook
